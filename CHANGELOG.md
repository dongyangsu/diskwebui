# 更新修复及优化一览

> 每个日期对应一版更新（版本号见括号）。可按日期逐条对照本版相对上一版**新增了 / 修复了 / 优化了 / 删除了**什么。
> 源码包归档在 [`versions/`](versions/)，当前可直接部署的源码在 [`src/`](src/)。

---

## 2026/09/24（v3.0 · 当前版本）

### 新增了
- 新增了机器列表**排序**功能：点表头在「升序 → 降序 → 取消」之间循环；按住 Shift 点表头可**追加次要条件**；右键表头可弹出菜单（升序 / 降序 / 追加为次要条件 / 筛选该列 / 自定义排序 / 清除排序）
- 新增了「**自定义排序**」弹窗：最多 **7 级**条件、每级可单独升/降序、每级可填**自定义序列**（例如让状态按「离线 → 在线」排）；并提供排序选项：中文按**拼音 / 笔画 / Unicode** 排、**区分大小写**、**空白值排在最后**
- 新增了**列筛选**（下拉勾选值、显示每个值的条数、可搜索值、全选/清空/反选、清除单列筛选）、**全局搜索**（命中字高亮，Ctrl+F 聚焦）、**快速筛选**（全部 / 在线 / 离线 / 未知 / 本机 / 多IP）
- 新增了「**命名视图**」：把排序 + 筛选 + 列布局 + 密度存成一个视图，可应用 / 设为默认 / 删除，也能「设为全站默认」
- 新增了**多用户各自保存**：列表偏好按用户名存到服务端，不同人互不干扰
- 新增了**批量操作**：行勾选列（含表头全选）、批量连接测试、批量删除、取消选择
- 新增了「**数据…**」统一入口，**导出 9 种格式**（CSV / TSV / Excel(.xls) / JSON / YAML / XML / HTML / Markdown / 纯文本对齐），范围可选「当前筛选结果 / 全部机器 / 仅选中行」；**导入自动识别** CSV / TSV / JSON / YAML / IP 列表 / `IP:端口` / Excel 粘贴内容，先解析预览再导入
- 新增了**打印**：独立的打印版表格（打印全量行，不受分页影响），自动带上标题、时间与当前排序/筛选说明，打印时隐藏工具栏、按钮、输入框与勾选列
- 新增了**分页**（100 / 200 / 300 / 500 / 1000 / 全部，默认 300，每页），数据量少于每页时自动隐藏分页器
- 新增了机器 IP 的**多地址徽标 + 悬停小窗**（列出该机全部网卡 IP，点地址即可复制）
- 新增了列表体验：**冻结表头**、斑马纹、悬停整行高亮、**条件格式**（离线淡红 / 未知淡黄 / 本机淡紫）、**密度切换**（紧凑/舒适）、双击行打开该机页面
- 新增了**键盘操作**：↑↓ 移动选中行、Enter 打开页面、空格勾选、Ctrl+C 复制选中行、Ctrl+F 搜索、Esc 清除排序与筛选
- 新增了 UI 回归测试套件 `src/scripts/ui_suite.mjs`（65 项断言，支持 HTTP 与 HTTPS 两种入口）

### 修复了（共 14 条）

**列表与交互类（6 条）**
- 修复了机器管理页点击『连接测试』『打开页面』后报「未知接口 /api/v1/test」的问题（路由被"机器作用域转发"规则抢先匹配）
- 修复了点击『同步机器列表』报「机器不存在（404）」的问题（`/machines/sync` 被当成机器 id）
- 修复了**隐藏列时表头与表体错位**的问题（表头此前仍渲染被隐藏的列）
- 修复了**筛选/搜索/切换视图后页码不重置**的问题（翻到第 5 页再筛选会出现空白页）
- 修复了**键盘上下移动选中行不能跨页**的问题（现会自动翻页）
- 修复了**「状态」列自定义排序失效**的问题（内部值与序列值口径不一致）

**服务端与接口类（4 条）**
- 修复了**机器 IP 校验过宽**的问题（`999.1.1.1` 这类非法 IP 可被新增，现按严格 IPv4 校验，每段 0–255）
- 修复了**列表偏好"保存成功却存不上"**的问题（服务端 PUT 请求体未被解析）
- 修复了**本机（管理端自己）那行「最后检测」时间停留在首次添加时间**的问题（现每次打开列表/测试都会刷新）
- 修复了**视图保存/应用报错**的问题（视图快照包含自身导致循环引用）

**打印与导出类（2 条）**
- 修复了**打印功能无法使用**的问题（打印函数内变量重名导致报错，已重写为独立打印版表格）
- 修复了**打印稿里混入"新增机器"输入框与页面标题**的问题

**页面与大规模数据类（2 条）**
- 修复了**未登录访问时页面产生大量 401 请求**的问题（现未登录不再发起鉴权请求，控制台无报错；并补充站点图标消除 404）
- 修复了**数据量较大时页面报错**的问题（分页渲染逻辑里变量重复声明，2 万行以上时整页脚本中断）

### 优化了
- 优化了**全站 5 个页面（硬盘 / 机器管理 / 命令行 / 全局日志 / 设置）的自适应**：页面本体现在**没有纵向（和横向）滚动条**，顶部栏固定，表格与面板在各自区域内滚动；硬盘页三栏布局随屏幕宽度自动变两栏/单栏；命令行终端铺满剩余高度
- 优化了**窄屏与高 DPI 适配**：新增多档媒体查询（≤1500 / ≤1200 / ≤900 / ≤860 / ≤600px），工具栏自动换行；高 DPI（1.25~3x）与触摸设备验证通过
- 优化了**大数据量渲染性能**：机器列表改为分页渲染后，2 万行渲染约 **98ms**、5 万行约 **66ms**（此前 5 千行需 8 秒以上）
- 优化了机器**编辑体验**：由"逐项弹窗问答"改为**一次性小表单**（5 个字段预填当前值，回车=保存，Esc=取消）
- 优化了**机器列表的 IP 展示**：主 IP + 「N 个 IP」徽标 + 其余 IP 折叠，悬停/点击查看全部（此前只能看到一个 IP）
- 优化了**打印样式**：打印只输出表格本体，版式更干净

### 删除了
- 删除了分散的「复制到 Excel」「导出 CSV」两个按钮（统一合并进「数据…」入口）
- 删除了机器编辑的逐项问答式弹窗流程（改为一次性表单）

---

## 2026/09/20（v2.0）

### 新增了
- 新增了**插盘自动检测 + 自动格式化 + 自动续格**（主从模型）：格完复检仍有缺陷才续格；「全局暂停」与「单盘截停」互不干扰
- 新增了**磁盘空间保护**（`lib/space.js`）：可用空间低于阈值时阻止格式化并提示
- 新增了**标签生成脚本** `scripts/make_label.py`（按控制器/硬盘生成测报标签）
- 新增了**机器维度接口**（`/machines/<id>/disks|jobs|formatting|terminal|autoformat…`）：选中哪台就作用于哪台
- 新增了**格式化历史**与**审计日志自动轮转**（日志过大自动裁剪）
- 新增了**多机自动同步（主源优先）**：主源可达时永不使用备用源，使用备用源会写审计
- 新增了自制**非阻塞弹窗**（`askModal / askConfirm / askPrompt`），替代浏览器原生 confirm/prompt
- 新增了**静态文件 no-cache**（改完前端即生效，无需清缓存）
- 新增了命令行**常驻工具会话**（hugo/wdckit 常开，减少反复启动）

### 修复了
- 修复了 v1.0 的 **14 个开发期问题**：自动检测 TDZ 崩溃、`-m` 参数误伤同型号盘、`already running` 死循环、长格式化任务被 3600 秒误判超时 等
- 修复了部署阶段暴露的 **9 个部署期问题**（详见文档第九、十章）

### 优化了
- 优化了硬盘识别与缺陷判定流程（`lib/detect.js`、`lib/rules.js` 重写为 443 行，支持按序列号记忆人工修正）
- 优化了工具执行层 `lib/exec.js`（555 → 1318 行）：PTY 多标签、命令确认应答、任务接管、格式化前缺陷快照
- 优化了清理逻辑 `lib/clean.js`（67 → 99 行）：分「系统日志」与「用户缓存」两步清理
- 优化了服务端 `server.js`（1201 → 1531 行）：三级角色鉴权（viewer/operator/admin）、机器作用域路由、调度器与审计

### 删除了
- 删除了 v1.0 中已废弃的临时/调试函数，由新模块统一替代

---

## 2026/09/16（v1.0 · 首版）

- 首版功能：多机管理 + 硬盘检测 + 缺陷判定 + 手工格式化 + 命令行
- 已知问题（已在 v2.0 修复）：自动检测 TDZ 崩溃、`-m` 误伤同型号盘、`already running` 死循环、长任务被 3600 秒误判超时 等共 14 项
