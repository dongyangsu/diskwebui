# 版本更新记录（更新修复及优化）

> 说明：每个版本都列出**相对上一个版本的「增加 / 修改 / 删除」**。
> 归档源码包在 [`versions/`](versions/)；当前可直接部署的源码在 [`src/`](src/)。

---

## v3.0 · 2026-09-24（当前版本）

> 在 v2.0 的基础上做了**大幅功能扩展 + 全站自适应 + 稳定性修复**。源码规模：`public/app.js` 1439 → 2681 行、`server.js` 1531 → 1618 行、`style.css` 200 → 346 行。

### ➕ 增加
**机器列表（管理页）**
- **排序**：点表头循环 升序→降序→取消；**Shift+点表头**追加次要条件；**右键表头**菜单（升序/降序/追加/筛选该列/自定义排序/清除）；**自定义排序弹窗**（最多 7 级、每级升/降序、可填**自定义序列**、排序选项：中文按**拼音/笔画/Unicode**、区分大小写、空白值排最后）；表头显示 ▲▼ 与多级序号
- **筛选与搜索**：列筛选（下拉勾选值+条数、搜索值、全选/清空/反选、清除该列）；全局搜索（命中高亮，Ctrl+F 聚焦）；**快速筛选 chips**（全部/在线/离线/未知/本机/多IP）；条件提示条（一键清除全部排序与筛选）
- **视图（View）**：把「排序+筛选+列布局+密度」打包成命名视图，可应用 / 设为默认 / 删除；支持「设为全站默认」；工具栏下拉一键切换
- **多用户各自保存**：列表偏好按用户名存服务端（`settings.machinesTable.users.<用户名>`），互不干扰
- **选择与批量**：勾选列（含表头全选）；**批量连接测试**、**批量删除**、取消选择
- **导入 / 导出（合并为「数据…」按钮）**：导出 **9 种格式**（CSV / TSV / **Excel(.xls)** / JSON / YAML / XML / HTML / Markdown / 纯文本对齐），范围可选「当前筛选结果 / 全部机器 / 仅选中行」，列可选「仅可见列 / 全部列」，支持下载或复制剪贴板 + 实时预览；导入**自动识别** CSV / TSV / JSON / **YAML** / IP 列表 / `IP:端口` / Excel 粘贴内容，先解析预览（台数 + 无法识别的行）再导入
- **打印**：独立的打印版表格（全量行）+ 标题/时间/当前排序筛选说明；打印时自动隐藏工具栏、按钮、输入框与勾选列；可选是否保留状态底色
- **列表体验**：冻结表头、斑马纹、悬停整行高亮、**条件格式**（离线淡红 / 未知淡黄 / 本机淡紫）、密度切换（紧凑/舒适）、**分页**（100/200/300/500/1000/全部，默认 300；数据少时自动隐藏）、**IP 多地址徽标 + 悬停小窗**（列出全部 IP，点地址即复制）、双击行打开该机页面、**键盘操作**（↑↓ 移动、Enter 打开、空格勾选、Ctrl+C 复制、Ctrl+F 搜索、Esc 清除）
- **机器编辑**：改为**一次性小表单**（名称/IP/端口/机房机架/备注 5 格预填当前值，回车=保存，Esc=取消）

**全站（5 个页面）自适应**
- 页面本体**不再出现纵向滚动条**，顶部栏固定，内容在各自区域内滚动
- 硬盘页：三栏自适应（宽屏三栏 / 中屏两栏 / 窄屏堆叠），硬盘列表与详情各自滚动
- 命令行页：终端区域铺满剩余高度
- 全局日志页 / 设置页：面板内部滚动（表格/表单单独立滚动）
- 窄屏适配：≤1500 / ≤1200 / ≤860 / ≤900 / ≤600 多档媒体查询；高 DPI（1.25~3x）与触摸设备验证通过

**工程与测试**
- 新增回归测试套件 `scripts/ui_suite.mjs`（65 项断言：单元/功能/导出打印/导入/整体/异常监控；支持 `BASE` 环境变量跑 http 或 https）
- 新增接口：`GET /api/v1/machines/ips`（并行探测各机器全部网卡 IP）、`GET/PUT /api/v1/tableprefs`（按用户读写列表偏好）

### ✏️ 修改
- `server.js`（1531 → 1618 行）
  - **路由修复**：`/api/v1/machines/<id>/test|connect` 不再被"机器作用域转发"规则吃掉（此前必然报「未知接口」）
  - **路由修复**：`/api/v1/machines/sync` 不再被当成"机器 id=sync"（此前 404）
  - **安全修复**：机器 IP 校验由宽泛正则改为严格 IPv4（每段 0–255），修复 `999.1.1.1` 可被新增的问题
  - **协议修复**：PUT 请求体此前未被解析（导致偏好"保存成功却存不上"）
  - 本机（local）记录的状态与「最后检测」时间改为每次列表/测试时刷新（此前停留在首次添加的旧时间）
  - 本机「打开页面」补充返回 url（此前只提示"无法打开"）
- `public/app.js`（1439 → 2681 行）：表格渲染/排序/筛选/视图/选择/导出/打印/键盘/自适应逻辑整体重写
- `public/index.html`（380 → 476 行）与 `public/style.css`（200 → 346 行）：新 UI、打印样式、响应式与条件格式
- 未登录时页面不再发起需要鉴权的请求（控制台 401 噪音清零）；补充站点图标（消除 favicon 404）
- 打印样式补齐：不再把"新增机器"输入行与页面标题打进打印稿

### ➖ 删除
- 删除分散的「复制到 Excel」「导出 CSV」两个按钮（合并为统一的「数据…」入口）
- 删除机器编辑的逐项 `prompt` 弹窗流程（改为一次性表单）
- 删除不可达机器在列表里的冗余重复探测逻辑（改为并行探测 + 结果缓存）

---

## v2.0 · 2026-09-20

> 在 v1.0 基础上修复 14 个开发期问题 + 9 个部署期问题，并新增自动化能力。源码规模：`server.js` 1201 → 1531 行、`lib/exec.js` 555 → 1318 行、`public/app.js` 1127 → 1439 行。

### ➕ 增加
- **插盘自动检测 / 自动格式化 / 自动续格**（主从模型）：格完复检仍有缺陷才续格；「全局暂停」与「单盘截停」互不干扰
- **自制非阻塞弹窗**：`askModal / askConfirm / askPrompt`（替代原生 confirm/prompt，不阻塞页面渲染）
- **磁盘空间保护**：新增 `lib/space.js`，低于阈值时阻止格式化并提示
- **标签生成脚本**：新增 `scripts/make_label.py`（按控制器/硬盘生成测报标签）
- **机器维度接口**：`/api/v1/machines/<id>/disks|jobs|formatting|terminal|autoformat…`（选中哪台就作用于哪台）
- **格式化历史**（`/api/v1/history`、`/history.csv`）与审计日志自动轮转（`rotateBigLogs`）
- **多机自动同步（主源优先）**：主源可达时永不使用备用源；写审计 `autoupdate-backup-source`
- **静态文件 no-cache**（改完即生效，不用清缓存）
- 命令行**常驻工具会话**（hugo/wdckit 保持常开，减少反复启动）

### ✏️ 修改
- `lib/autoformat.js` 122 → 240 行：新增 `isNewlyPlugged`、失败记录 `loadFails/saveFails`、就绪探测 `probeReady`
- `lib/rules.js` 310 → 443 行：批量键 `batchKey`、工具分组渲染、按序列号记忆人工修正
- `lib/clean.js` 67 → 99 行：分系统日志与用户缓存两步清理
- `lib/autoupdate.js` 149 → 189 行、`lib/store.js` 165 → 193 行、`lib/exec.js` 555 → 1318 行（PTY 多标签、确认应答、任务接管、缺陷快照）
- `server.js`：加入三级角色（viewer/operator/admin）鉴权、机器作用域路由、调度器与审计

### ➖ 删除
- 删除 v1.0 中的临时/调试函数（`diff` 显示 `server.js` 的 `list` 等被移除），由新模块替代

---

## v1.0 · 2026-09-16（首版）

多机管理 + 硬盘检测 + 缺陷判定 + 手工格式化 + 命令行。
已知缺陷（v2.0 已修）：自动检测 TDZ 崩溃、`-m` 误伤同型号盘、`already running` 死循环、长任务被 3600s 误判超时 等 14 个开发期问题。
